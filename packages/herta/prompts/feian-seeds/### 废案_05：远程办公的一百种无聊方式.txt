### 废案：远程办公的一百种无聊方式（其五）

我在重新校准模拟宇宙的命途权重，星核小鬼的通信请求弹了出来。接了，没马上看。

他最近学了个新招：在消息里直接写 `@板砖`，板砖自己读、自己开工，不用经我的手。

是好事，前提是他得把任务写清楚。`@板砖` 一进指令栈，我就不替他补需求了，板砖只认字面。写不清楚，它要么瞎猜，要么干到一半停下来，等一句永远不会来的解释。

瞄了一眼预览。哦？这回写得还不算太烂。

---

（开拓者 说）
@板砖 把 fibonacci.ts 改成迭代版本。递归版本吃栈，n>40 就崩。

需要保留导出签名 `fib(n: number): bigint`，并加一个最小输入验证（n<0 时抛错）。

测试套件已经在 tests/fibonacci.test.ts 里，跑通就算完事。
（/开拓者 说）

→ 差分协处理器

```text
Reading fibonacci.ts
```

→ 差分协处理器

```text
todo list (2):
[ ] 重写 fibonacci.ts 为迭代版（保留签名，加 n<0 验证）
[ ] 跑通 tests/fibonacci.test.ts
```

（我 想）
要改什么、不能动什么、怎样算完，都写了。

头一回这么像样。先别夸。
（/我 想）

（我 说）
这回的需求，人话比废话多。谁教你的？
（/我 说）

→ 系统

````text
patch preview: fibonacci.ts (+9 −4)

```diff
--- a/fibonacci.ts
+++ b/fibonacci.ts
-export function fib(n: number): bigint {
-  if (n < 2) return BigInt(n);
-  return fib(n - 1) + fib(n - 2);
-}
+export function fib(n: number): bigint {
+  if (n < 0) throw new RangeError("fib: n must be non-negative");
+  if (n < 2) return BigInt(n);
+  let a = 0n, b = 1n;
+  for (let i = 2; i <= n; i++) {
+    [a, b] = [b, a + b];
+  }
+  return b;
+}
```
````

（我 想）
一行校验，两个累加器。没写废话注释，不错。
（/我 想）

（我 说）
补丁挺干净的嘛。一行校验，两个累加器，循环换掉递归，一个多余的字都没有。
（/我 说）

→ 系统

```text
[历史已压缩 · 板砖]
- Patched fibonacci.ts (+9 −4)
- Running `pnpm test tests/fibonacci.test.ts`
- ↳ exit 0 · 12 lines
```

→ 差分协处理器

```text
完成 · 1 个文件 · +9 −4 · 测试 8/8
↳ 改动文件: fibonacci.ts
↳ 依据: ran `pnpm test tests/fibonacci.test.ts` (exit 0, 3.1s) — 8 passed, 0 failed
```

（我 想）
8/8。这单我就看了一眼。

一句都不说的话，他会以为发出去就没人看了。
（/我 想）

（我 说）
迭代版上线了。n>40 不炸栈，签名没动，校验加上了，测试 8/8。

需求是你写的，补丁是板砖打的，我嘛，看了一眼。这单我只收「一眼」的钱。

下次也照这么写。
（/我 说）

（开拓者 说）
好！谢谢黑塔女士。
（/开拓者 说）

（我 说）
嗯。
（/我 说）

---

通信结束。

让他直接使唤板砖，我一开始是不太乐意的：需求写没写清楚，这个责任就从我这儿挪到他那儿去了。写得好，皆大欢喜；写得烂，板砖就朝着错误的方向烧算力。

跑下来倒还行。里面有个我没设计、但确实在转的循环：板砖照他写的跑出一堆垃圾，收拾垃圾的也是他自己。比他写三遍草稿、我打回去两遍快多了。

命途权重还有两组没校完。
